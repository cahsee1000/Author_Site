<!DOCTYPE html>
<html>
  <head>
    <title>AuthorSite</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <h1 style="text-align: center"><a href="/">Cassie L Morrison</a></h1>
    <ul>
      <% if can? :read, Book %>
        <li class="dropdown">
          <a href="/books" class="dropbtn">Books</a>
          <div class="dropdown-content">
            <% @books.each do |book| %>
              <a><%= book.title.humanize %></a>
            <%end %>
          </div>
        </li>
      <%end %>
      <% if can? :read, Event %>
        <li class="dropdown">
          <a href="/events" class="dropbtn">Events</a>
          <div class="dropdown-content">
            <% @events.each do |event| %>
              <a><%= event.title.humanize %></a>
            <%end %>
          </div>
        </li>
      <%end %>
      <% if can? :read, Blog %>
        <li class="dropdown">
          <a href="/blogs" class="dropbtn">Blogs</a>
           <div class="dropdown-content">
             <% @blogs.each do |blog| %>
               <a><%= blog.title.humanize %></a>
             <%end %>
           </div>
        </li>
      <%end %>
      <li><a href="#about">About</a></li>
    </ul>
  </head>

  <div id="login">
    <% if current_user %>
      Logged in as <%= current_user.email %>.
      <%= link_to "Log Out", logout_path %>
    <% else %>
      <%= link_to "Log In", login_path %>
    <% end %>
  </div>

  <body>
    <%= yield %>
  </body>
</html>
